# Runtime Evidence

`workspace/runtime/` stores local runtime evidence captured during validation.

Current subareas:

- `gameplay/`: gameplay screenshots and other direct validation snapshots

Keep long-lived docs and reusable tooling outside this directory.
